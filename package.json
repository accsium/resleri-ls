{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "rm -rf public/ && npm run copy-public && vite build",
    "preview": "vite preview",
    "data": "node scripts/resolve.cjs",
    "prepare-data": "node scripts/translate.cjs",
    "selection": "node scripts/select.cjs",
    "data-conversion": "rm -rf data/output/ data/selection/ && npm run selection && npm run prepare-data && npm run data",
    "copy-public": "node scripts/copy-public.cjs"
  },
  "dependencies": {
    "marked": "^18.0.4",
    "vue": "^3.4.0",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "playwright": "^1.60.0",
    "typescript": "^6.0.3",
    "vite": "^5.4.0"
  }
}
