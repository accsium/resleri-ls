{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "data": "node scripts/resolveAndSplit.cjs",
    "prepare-data": "node scripts/translations.cjs",
    "prebuild": "npm run prepare-data && npm run data"
  },
  "dependencies": {
    "marked": "^18.0.4",
    "vue": "^3.4.0",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "playwright": "^1.60.0",
    "vite": "^5.4.0"
  }
}
